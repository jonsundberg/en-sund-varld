import { defineConfig } from 'astro/config';

export default defineConfig({
  // Uppdatera när du har en riktig domän, t.ex.:
  // site: 'https://ensundvarld.se',
  // eller sätt till din Vercel-URL efter första deploy
});
