import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://en-sund-varld.vercel.app',
  // Uppdatera till egen domän när den finns, t.ex.:
  // site: 'https://ensundvarld.se',
});
